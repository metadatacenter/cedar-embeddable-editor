{
  "name": "cee-visual",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Visual regression baseline for the CEDAR Embeddable Editor web component",
  "scripts": {
    "bundle": "cat ../dist/cedar-embeddable-editor/runtime.js ../dist/cedar-embeddable-editor/polyfills.js ../dist/cedar-embeddable-editor/main.js > public/cedar-embeddable-editor.js",
    "fixtures": "node generate-fixtures.mjs && rm -rf public/fixtures public/served && cp -r fixtures public/fixtures && cp -r fixtures/served public/served",
    "prepare:all": "npm run bundle && npm run fixtures",
    "test": "node check-bundle-fresh.mjs && playwright test",
    "update": "node check-bundle-fresh.mjs && playwright test --update-snapshots",
    "report": "playwright show-report",
    "check-fresh": "node check-bundle-fresh.mjs",
    "flake-hunt": "node check-bundle-fresh.mjs && for i in $(seq 1 ${RUNS:-20}); do echo \"--- run $i\"; playwright test --retries=0 --reporter=line || exit 1; done"
  },
  "dependencies": {
    "cedar-model-typescript-library": "npm:@org.metadatacenter/cedar-model-typescript-library@0.9.2-dev.20260804.f1a3784"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
